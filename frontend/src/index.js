import React from 'react';
import ReactDOM from 'react-dom';
import App from './main/App'
import Header from './components/Header'

ReactDOM.render(
    [ < Header title = "Carona Já" / > , < App / > ],
    document.getElementById('root')
);